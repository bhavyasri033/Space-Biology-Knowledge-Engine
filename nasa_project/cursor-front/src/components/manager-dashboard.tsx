'use client';

import DynamicManagerDashboard from './dynamic-manager-dashboard';

export default function ManagerDashboard() {
  // Always render the dynamic dashboard as the default
  return <DynamicManagerDashboard />;
}

